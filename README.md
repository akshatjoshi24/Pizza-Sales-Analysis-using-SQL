📌 Project Overview

This project analyzes Pizza Sales Data using SQL.
The dataset contains multiple tables such as orders, order_details, pizzas, and pizza_types.
The goal is to extract actionable business insights from raw sales data through SQL queries.

🎯 Objectives

Understand sales trends across different pizza categories and sizes.
Calculate total revenue and average order value.
Identify top-selling pizzas and their contribution to revenue.
Analyze percentage revenue share of each pizza category.
Practice SQL concepts from Basic → Intermediate → Advanced level.

🛠️ Tools & Skills Used

SQL (MySQL / PostgreSQL / any RDBMS you used)
Joins (INNER JOIN, JOIN with multiple tables)
Aggregations (SUM, COUNT, AVG, GROUP BY, ORDER BY)
Subqueries & Common Table Expressions (CTEs)
Window Functions (RANK, ROW_NUMBER, PARTITION BY)

📈 Key Insights

Classic pizzas contributed the highest % revenue.
Supreme & Chicken categories followed closely.
Evening hours had the maximum order volume.
Few pizzas generated majority of revenue (Pareto principle observed).
